# ProSEO Master - Service Configuration
# PrestaShop 8.x Symfony DI
# Note: Main configuration uses getContent() for better compatibility

services:
    _defaults:
        public: true
        autowire: false
        autoconfigure: false

    # Services can be added here for future Symfony integration
    # Currently the module uses getContent() for the main admin interface
