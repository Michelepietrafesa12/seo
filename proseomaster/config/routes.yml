# ProSEO Master - Routes Configuration
# PrestaShop 8.x Symfony routing
# Note: Main configuration uses getContent() for better compatibility
# Routes can be added here for future AJAX endpoints if needed
